import 'package:sdk/flutter_sdk.dart';

class BusinessDependencies {
  static Future setup(GetIt injector) async {
    //  injector.registerFactory<INewsBusiness>(() => NewsBusiness(injector()));
  }
}
