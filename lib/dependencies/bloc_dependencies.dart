import 'package:sdk/flutter_sdk.dart';

class BlocDependencies {
  static Future setup(GetIt injector) async {
    // injector.registerFactory<HomeBloc>(() => HomeBloc(injector(), injector(), injector(), injector(), injector(), injector()));
  }
}
